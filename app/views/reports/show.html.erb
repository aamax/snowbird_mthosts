<% if @report == 'confirmations' %>
    <%= render 'confirmations' %>
<% elsif @report == 'phone_list' %>
    <%= render 'phone_list' %>
<% elsif @report == 'shifts_by_host' %>
    <%= render 'shifts_by_host' %>
<% else %>

    <div class='hero-unit'>
        Sorry, the <%= @report %> Report is not available.
    </div>

<% end %>